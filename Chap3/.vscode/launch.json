{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Active CUDA (Chap3, preserve path)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder:root}/output/${workspaceFolderBasename}/${relativeFileDirname}/${fileBasenameNoExtension}.exe",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal",
      "preLaunchTask": "Build Active CUDA (Chap3, preserve path)"
    }
  ]
}
